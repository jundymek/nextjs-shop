const title = "Next sklep";
const description = "Sklep nextjs";

const nextSeoConfig = {
  title,
  description,
  openGraph: {
    title,
    description,
    site_name: "Next sklep",
  },
};

export default nextSeoConfig;
